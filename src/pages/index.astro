---
import { getCollection } from "astro:content";
import FormattedDate from "../components/FormattedDate.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { SITE_DESCRIPTION, SITE_TITLE, SITE_SUBTITLE } from "../consts";

const blogPosts = await getCollection("blog");
const zuegPosts = await getCollection("zueg");

const allPosts = [...blogPosts, ...zuegPosts].sort((a, b) => {
    if (a.data.pinned && !b.data.pinned) return -1;
    if (!a.data.pinned && b.data.pinned) return 1;
    return b.data.pubDate.valueOf() - a.data.pubDate.valueOf();
});

const recentPosts = allPosts.slice(0, 5);
---

<BaseLayout title={SITE_TITLE} description={SITE_DESCRIPTION}>
    <main>
        <div class="container">
            <!-- Site intro -->
            <section class="hero">
                <h1 class="hero-title">👋 欢迎来到 Neomelt 的博客</h1>
                <div class="hero-bio">
                    <p>
                        你好，我是 <strong>Neomelt</strong>。一个喜欢折腾的工科生，对计算机视觉、机器人和系统编程感兴趣。
                    </p>
                    <p>
                        这里是我记录学习过程、技术思考和日常随笔的地方。内容可能涉及 ROS2、Rust、C++、深度学习，以及一些不那么技术的东西。
                    </p>
                    <p class="hero-links">
                        <a href="https://github.com/Neomelt" target="_blank" rel="noopener" class="hero-link">GitHub</a>
                        <span class="hero-sep">·</span>
                        <a href="/posts" class="hero-link">全部文章</a>
                        <span class="hero-sep">·</span>
                        <a href="/posts/archive" class="hero-link">归档</a>
                    </p>
                </div>
            </section>

            <!-- Recent posts -->
            <section>
                <div class="section-header">
                    <h2 class="section-heading">最近文章</h2>
                    <a href="/posts" class="view-all">全部文章 →</a>
                </div>

                <div class="post-list">
                    {recentPosts.map((post) => {
                        const isZueg = post.collection === "zueg";
                        const url = isZueg ? `/posts/zueg/${post.id}` : `/posts/${post.id}`;
                        return (
                            <a href={url} class="post-card">
                                <div class="post-card-meta">
                                    <FormattedDate date={post.data.pubDate} />
                                    {isZueg && <span class="tag">随笔</span>}
                                    {post.data.pinned && <span class="badge-pinned">置顶</span>}
                                </div>
                                <div class="post-card-title">{post.data.title}</div>
                                {post.data.description && (
                                    <div class="post-card-desc">{post.data.description}</div>
                                )}
                            </a>
                        );
                    })}
                </div>
            </section>
        </div>
    </main>
</BaseLayout>

<style>
    .hero {
        padding: 3rem 0 2.5rem;
        border-bottom: 1px solid var(--border);
        margin-bottom: 3rem;
    }

    .hero-title {
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        color: var(--text);
        line-height: 1.25;
    }

    .hero-bio {
        max-width: 600px;
    }

    .hero-bio p {
        font-size: 1rem;
        color: var(--text-muted);
        line-height: 1.8;
        margin-bottom: 0.9rem;
    }

    .hero-bio strong {
        color: var(--text);
        font-weight: 600;
    }

    .hero-links {
        margin-top: 1.5rem;
        margin-bottom: 0 !important;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        flex-wrap: wrap;
    }

    .hero-link {
        color: var(--accent);
        text-decoration: none;
        font-size: 0.95rem;
        font-weight: 500;
        transition: color var(--transition);
    }

    .hero-link:hover {
        color: var(--accent-hover);
        text-decoration: underline;
    }

    .hero-sep {
        color: var(--border);
        font-size: 0.9rem;
    }

    .section-header {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        margin-bottom: 0.75rem;
    }

    .view-all {
        font-size: 0.875rem;
        color: var(--text-muted);
        text-decoration: none;
        transition: color var(--transition);
    }

    .view-all:hover {
        color: var(--accent);
    }
</style>
